import { describe, it } from "vitest";

describe("StringCalculator", () => {
  it.todo("renders app title as String Calculator inside h1 tag");

  it.todo("submits the input and renders the result when click on the button");

  it.todo("submits the input and renders the result when click enter key");

  it.todo("displays the error in case of negative number");

  it.todo("removes the result and error in case user inputs anything");
});
